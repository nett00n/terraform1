name: Repo 2 Workflow

on:
  repository_dispatch:
    types: [trigger-workflow]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo 2 Code
        uses: actions/checkout@v3

      - name: Checkout Repo 1 Code
        uses: actions/checkout@v3
        with:
          repository: nett00n/app1
          token: ${{ secrets.PAT_TOKEN }}
          path: ./repo1

      # Add additional steps to use code from Repo 2
      - name: Run Your Code
        run: |
          echo "Repo 2 code is running"
          pwd
          find .
